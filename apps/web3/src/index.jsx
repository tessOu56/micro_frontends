import { Button } from "@my-org/ui";
import React from "react";
import ReactDOM from "react-dom/client";
const App = () => {
  return <h1>Hello from Web3!  <Button /> </h1>;
};

ReactDOM.createRoot(document.getElementById("root")).render(<App />);